# 复制为 application-secrets.yml 并填入你的 InfluxDB Token，该文件不要提交到 Git
# 复制命令（在 NetPulse/src/main/resources/ 下）：cp application-secrets.yml.example application-secrets.yml
influxdb:
  token: 你的InfluxDB_API_Token
