spring:
  datasource:
    password: ${MYSQL_PASSWORD:root123456}
  data:
    redis:
      password: ${REDIS_PASSWORD:root123456}
  rabbitmq:
    password: ${RABBITMQ_PASSWORD:root123456}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}

auth:
  token-secret: ${AUTH_TOKEN_SECRET:netpulse-dev-secret-please-change}

influxdb:
  token: ${INFLUXDB_TOKEN:nebula_token_2026}
